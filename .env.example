# Copy this file to .env and fill in your values. Never commit .env.
# Primary LLM: DeepSeek V3 via OpenRouter (free tier, best performance)
# Sign up at openrouter.ai → Keys → Create key (no card needed for free tier)
OPENROUTER_API_KEY=
# Legacy fallback
GROQ_API_KEY=
ADMIN_KEY=
